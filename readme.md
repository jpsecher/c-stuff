# C/C++ stuff

Various scratch-pad routines with unit tests!

## One-time setup

### MacOS

    $ brew install cpputest

### Debian-ish

    $ sudo apt -y install libcpputest-dev

## Usage

    $ cd network
    $ make
    $ ./test
    $ make clean
